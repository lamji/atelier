node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.*
*.log
.DS_Store
coverage/
.atelier-data/
apps/web/.vite/
.loveai/
.atelier/

# electron-builder output — installers and unpacked binaries, never commit
apps/desktop/release/

# esbuild output of the agent, regenerated by apps/agent/scripts/bundle-electron.mjs
# (desktop dev spawns it in --watch mode; `pnpm dist` rebuilds it via build-backend.mjs)
apps/agent/dist-electron/

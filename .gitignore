node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.*
*.log
.DS_Store
coverage/
.atelier-data/
apps/web/.vite/
.loveai/
.atelier/

# electron-builder output — installers and unpacked binaries, never commit.
# Both spellings: the path this repo builds into, and the name anywhere else.
apps/desktop/release/
release/
*.exe
*.exe.blockmap
*.asar

# Framework build output and caches. apps/landing/.next was COMMITTED before
# these rules existed — 369 files and 116 MB of turbopack .sst blobs — which
# is most of what makes this repo expensive to clone.
.next/
.turbo/
.vite/
.cache/
out/
*.tsbuildinfo

# esbuild output of the agent, regenerated by apps/agent/scripts/bundle-electron.mjs
# (desktop dev spawns it in --watch mode; `pnpm dist` rebuilds it via build-backend.mjs)
apps/agent/dist-electron/
